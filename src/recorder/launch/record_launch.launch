<?xml version="1.0"?>
<launch>

  <arg name="dir" default="$(env HOME)/usb_cam_video"/>

  <!-- usb cam launch -->
  <arg name="usb_cam" default="false"/>
  <group if="$(arg audio)">
    <include file="$(find usb_cam)/launch/usb_cam-test.launch">
      <arg name="device" value="/dev/video0" />
    </include>
  </group>

  <!-- video_record -->
  <arg name="video" default="false"/>
  <group if="$(arg video)">
    <include file="$(find image_view)/launch/video_capture_launch.launch">
      <arg name="dst" value="$(arg dir)" />
    </include>
  </group>

  <!-- audio_record -->
  <arg name="audio" default="false"/>
  <group if="$(arg audio)">
    <include file="$(find console_backend)/launch/capture_to_file.launch">
      <arg name="device" value="hw:1,0"/>
      <arg name="dst" value="$(arg dir)" />
    </include>
  </group>

</launch>
